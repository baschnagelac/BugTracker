@model TicketDevViewModel


@{
    ViewData["Title"] = "Assign Ticket Developer";
}


<div class="d-flex justify-content-center mt-5 flex-column align-items-center">
    <h3 class="my-5">Assign Project Members</h3>
    <div class="card">
        <div class="card-header">
            <div class="card-title">@Model.Ticket!.Title</div>
        </div>
        <div class="card-body">
            <form asp-action="AssignProjectMembers" asp-controller="Projects" method="post" class="d-flex flex-column">
                <input type="hidden" asp-for="Ticket!.Id" />
                <select asp-items="Model.DevList" asp-for="SelectedDev" class="form-control" required>
                </select>
                <span asp-validation-for="SelectedDev" class="text-danger"></span>
                <button type="submit" class="btn btn-primary my-3">Save</button>
                <a asp-action="Details" asp-route-id="@Model.Ticket?.Id" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>